// Write a JavaScript function toCamelCase that converts a given string to camelCase.
// The function should handle strings with spaces, hyphens, underscores, or mixed separators.
// It should ignore leading/trailing whitespace, convert all words after the first to start with an uppercase letter,
// and lowercase everything else.
// Include error handling: if the input is not a string (e.g., a number, array, null, or undefined),
// throw a TypeError with a descriptive message.
